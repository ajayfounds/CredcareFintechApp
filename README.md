
  # CredCare -make

  This is a code bundle for CredCare -make. The original project is available at https://www.figma.com/design/4vk1jM5ixMIAXWfkiFTgWU/CredCare--make.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  